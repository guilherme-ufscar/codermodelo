services:
  axyr-nexus:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: axyr-nexus
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3000}:80"
    environment:
      - TZ=America/Sao_Paulo

networks:
  default:
    name: axyr-network
